metadata:
        format: Lava-Test Test Definition 1.0
        name: lttng-fuzzing-kprobe-test
        description: "Run kprobe fuzzing test suite"
install:
        git-repos:
                - url: https://github.com/frdeso/lttng-ci
                  destination: lava_attach_fuzzing_commands
                  branch: master
        steps:
                - export TMPDIR="/tmp"
                - cd
                - ulimit -c unlimited
                - mkdir -p coredump
                - touch lttng-enable-event.sh
                - echo "$(pwd)/coredump/core.%e.%p.%h.%t" > /proc/sys/kernel/core_pattern
run:
        steps:
                - source /root/lttngvenv/activate
                - cd ci/
                - lava-test-case run-tests --shell "./scripts/system-tests/kprobe-fuzzing.sh"
                - tar czf coredump.tar.gz coredump
                - lava-test-case-attach run-tests coredump.tar.gz
                - lava-test-case-attach run-tests lttng-enable-event.sh
